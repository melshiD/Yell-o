<% layout('layouts/boilerplate') %>

    <h1>
        <%= campground.title %>
    </h1>
    <h2>
        <%= campground.location %>
    </h2>
    <img src="<%=campground.image%>" alt="image">
    <p><%= campground.description  %> </p>
    <a href="/campgrounds">Back To Main</a>
    <br>
    <a href="/campgrounds/<%= campground._id %>/edit">Edit Site Entry</a>
    <form action="/campgrounds/<%=campground.id%>?_method=DELETE" method="POST">
        <button>DELETE</button>
    </form>
